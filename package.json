{
  "name": "electron-with-create-react-app",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "electron": "^1.4.14",
    "wait-on": "^2.0.2"
  },
  "dependencies": {
  },
  "homepage": "./",
  "main": "src/electron-starter.js",
  "scripts": {
    "electron": "electron .",
    "start": "wait-on http://localhost:3000/ && ELECTRON_START_URL=http://localhost:3000/ npm run electron",
    "exocortex": "ELECTRON_START_URL=https://app.exocortex.ai npm run electron",
    "muse-eeg": "ELECTRON_START_URL=http://muse-eeg-app.firebaseapp.com npm run electron"
  }
}
